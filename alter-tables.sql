USE metro_cdmx;

-- ALTER TABLE `Stations`
-- MODIFY `id` BIGINT(20) UNSIGNED NOT NULL AUTO_INCREMENT,
-- ADD PRIMARY KEY(id),
-- ADD CONSTRAINT `Trains_Lines_id_Foreign`
-- FOREIGN KEY (`Line_id`) REFERENCES `Lines`(`id`);